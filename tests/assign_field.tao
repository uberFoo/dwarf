struct Foo {
    a: i32,
}
fn main() -> bool {
    let foo = Foo { a: 69 };
    foo.a = 42;

    print(foo.a + "\n");

    let a: bool = chacha::assert_eq(foo.a, 42);
    let b = chacha::assert_eq(foo, Foo { a: 42 });

    a && b
}
