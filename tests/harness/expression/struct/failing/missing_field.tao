struct Object {
    id: Uuid,
    name: String,
    description: String,
}

fn main() {
    // Specifying the type of the lhs forces a type check.
    let object: Object = Object {
        id: Uuid::new(),
        name: "Merlin",
        // description: "A wizard",
    };
}
