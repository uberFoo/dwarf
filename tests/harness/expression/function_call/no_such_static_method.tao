struct Point {
    x: int,
    y: int,
}

impl Point {
    fn new(x: int, y: int) -> Point {
        Point { x: x, y: y }
    }

    fn x(self) -> int {
        self.x
    }

    fn y(self) -> int {
        self.y
    }
}

fn main() {
    let p = Point::default();
}
