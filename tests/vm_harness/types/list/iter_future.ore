async fn main() -> Future<()> {
    let task = chacha::spawn(async || -> string { "Hello, world" });
    let tasks = [task];
    tasks.push(chacha::spawn(async || -> string { "Hello, world" }));

    for task in tasks {
        print(task.await);
    }

    ()
}
