struct Foo {}

fn main() {
    // Note all this muck. I have to create the list with an initial value.
    // And I can't use something like [Foo::{}, Foo::{}] because it breaks.
    let foo = Foo {};
    let foos = [foo];
    foos.push(Foo {});

    for foo in foos {}
}
