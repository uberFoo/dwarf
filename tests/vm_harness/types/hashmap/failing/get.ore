use std::option::Option;

fn main() {
    let map = { {} };

    map.insert("key", "value");
    let value = map.get("key");

    chacha::assert_eq(value, Option::Some("value"));

    let none = map.get("nonexistent");
    chacha::assert_eq(none, Option::None);
}
