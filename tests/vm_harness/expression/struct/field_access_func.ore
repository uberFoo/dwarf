struct Foo {
    x: int,
}

impl Foo {
    fn test(self, y: int) -> int {
        self.x + y
    }
}

fn main() {
    let foo = Foo { x: 1 };
    print("${foo.test(2)}");
    chacha::assert(foo.test(2) == 3);
}
