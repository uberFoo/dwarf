fn main() {
    chacha::assert_eq(1 + 1, 2);
    chacha::assert_eq(1.0 + 2.0, 3.0);
    chacha::assert_eq(true + true, true);
    chacha::assert_eq(true + false, true);
    chacha::assert_eq(false + false, false);
    chacha::assert_eq("foo" + "bar", "foobar");
}
