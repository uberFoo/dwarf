fn fib2(n: int) -> int {
    if n <= 1 {
        1
    } else {
        fib2(n - 1) + fib2(n - 2)
    }
}

fn main() -> int {
    print("Calculating the 17th fibonacci number:\n");
    let result = fib2(17);
    print(result as string + "\n");
    result
}
